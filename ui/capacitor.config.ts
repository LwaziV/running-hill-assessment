import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'running.hill.assessment',
  appName: 'running-hill-assessment',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;

export interface WordTypes {
    _id: string;
    name: string;
}

export class WordsFromType {
    _id: string;
    name: string;
}

export interface Sentences {
    _id: string;
    sentence: string;
}